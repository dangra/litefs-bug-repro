exec: sleep inf

fuse:
  dir: "./dbs-replica"

data:
  dir: "./.litefs-replica"

lease:
  type: "static"
  hostname: "localhost:20300"
  advertise-url: "http://localhost:20300"
  candidate: false

http:
  addr: ":20301"
